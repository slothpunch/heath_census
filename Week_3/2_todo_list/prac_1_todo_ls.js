// 1. Create variables: taskInput, addTaskBtn, taskList, clearCompletedBtn

// 2. Create an array called tasks to store tasks

// 3. Create a function called addTask to add tasks to the array tasks
    // Take the value from taskInput and trim it

    // Check if the input is empty. If not, add it to the array, reset the input and display tasks


// 4. Create a function called displayTasks to display all tasks
    // Set taskList HTML empty 

    // Display tasks with index  

    // Create li elements
    // input type is checkbox, id is task-index, use a ternary operator to check the completion of a task
    // label is task-index and show task text. 

    // Select input with querySelector and 
    // When a toggle box changes, it triggers the toggleTask() function 


// 5. Create a function called toggleTask that 

    // It helps by selecting the checkbox regardless. 
    // If selected, then it will mark that particular task as completed.
    // After that, displat the tasks

// 6. Create a function called clearCompletedTasks that removes the checked boxes and leaves the unchecked boxes.
// And display the tasks 


// When addTaskBtn is clicked, execute the addTask function
// When clearCompletedBtn is clicked, execute the clearCompletedTasks function
// Display tasks
